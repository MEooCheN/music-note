/* ==========================================================================
 * 模块 E：标注工作台
 * 目标：把和声、曲式、复调三块知识，合成一条能实操、能交付的标注流水线。
 * ========================================================================== */
(function () {
  'use strict';

  SITE.track({
    id: 'annotation', title: 'E · 标注工作台', short: '标注', en: 'Annotation Workbench',
    intro: 'DCML 和声标注是主线——它把罗马数字、数字低音、乐句边界、终止式全部编码进同一条标签。'
  });

  /* ------------------------------------------------------------------ E1 */
  SITE.unit({
    id: 'E1', code: 'E1', track: 'annotation', order: 1, minutes: 200,
    title: 'DCML 标签语法：一条标签能装多少信息',
    en: 'The DCML Harmony Label Syntax',
    goal: '看到任意一条 DCML 标签能读懂它，并能自己写出符合官方正则的标签。',

    flow: {
      from: 'A1 的数字低音与转位、A2 的终止式判据，以及 C 模块的乐句与功能段',
      why: '到这里你已经分别学完了三块：和声（A）、曲式（C），加上你自己的复调训练。' +
           '<b>但真实的标注工作不是把三套笔记各背一遍，而是把三层信息压进同一条机器可读的标签里</b>——' +
           'DCML 标准正是为这件事设计的：罗马数字（和声）+ 数字低音（转位）+ 终止式与乐句（曲式），全在一条标签上。' +
           '这件事卡住的是后面两件事：<b>E2 的曲式结构标注</b>要求你交出程序能解析的边界与终止式数据，' +
           '格式不对就等于没标；<b>E4 的个人素材库</b>要用同一套字段给每条素材记和声语境，' +
           '不会写标签，库里存的就只是一堆文件名。' +
           '还有一层理由：这套标准有官方正则，写错会被直接拒绝——' +
           '对一个自学的人来说，这是少见的"能自己判对错"的领域，值得先把语法磨准。',
      to: 'E2 曲式结构标注：把乐句边界、终止式、主题材料写成可交付的数据——用的就是这一节学的标签语法。'
    },

    sections: [
      { type: 'prose', title: '为什么这套标准值得学', html:
        '<p>DCML（École Polytechnique Fédérale de Lausanne 数字与认知音乐学实验室）的和声标注标准，' +
        '是目前公开可用的、最严格的一套罗马数字标注规范。它的价值在于：</p>' +
        '<ul>' +
        '<li><b>它是机器可读的</b>。有一条官方正则表达式，写错语法会被直接拒绝——你不需要"感觉对了"。' +
        '对自学的人这一点尤其值钱：没有老师判卷，正则就是判卷人。</li>' +
        '<li><b>它有真实语料</b>。Annotated Beethoven Corpus 用这套标准标注了贝多芬全部弦乐四重奏，' +
        '所以你的第一个练习对象不是虚构例题，而是可以逐条对照的专家标注。</li>' +
        '<li><b>它把三块知识缝在一起</b>：罗马数字（和声）+ 数字低音（转位）+ 终止式与乐句（曲式）' +
        '全部编码在同一条标签里。<b>这三样你都已经学过了，缺的只是"怎么写成一行"。</b></li>' +
        '</ul>' +
        '<p><b>读这一节需要 A1 的转位知识</b>：<code>figbass</code> 字段就是数字低音，' +
        '不认转位就读不懂标签。也需要 A2 的终止式判据（<code>|PAC</code> 这类写法靠它）' +
        '与 C 模块的乐句概念（<code>{</code>、<code>}</code> 标的就是乐句边界）。' +
        '<b>这三条里有哪条想不起来，先回去重看</b>——下面每一个字段都踩在它们上面。' +
        '另外提醒一句：<code>changes</code> 字段（括号里的数字）<b>不是</b>数字低音，' +
        '它数的是"从根音往上"，跟转位无关——这一条几乎每个从数字低音转过来的人都会搞错，' +
        '下面术语表里专门讲。</p>' },

      { type: 'prose', title: '标签的顺序是固定的：调 → 持续音 → 和弦 → 终止式 → 乐句标记', html:
        '<p>官方正则把一条标签拆成<b>固定顺序</b>的几个部分。顺序不能换，这正是它能被程序解析的原因：</p>' +
        '<p><code>(全局调.)?(局部调.)?(持续音[)? 和弦 ]? (|终止式)? (乐句标记)?</code></p>' +
        '<p>其中<b>只有「和弦」里的罗马数字是必需的</b>，其余全部可选。官方技术规格的原话是：' +
        '每个和声标签必须表达的唯一特征，就是那个（罗马数字的）根。和弦部分再展开：</p>' +
        '<p><code>罗马数字 + 和弦形态? + 数字低音? + (变化音)? + /相对根?</code></p>' +
        '<p>两个最容易读漏的位置：① <b>持续音的 <code>]</code> 紧跟和弦，排在终止式与乐句标记之前</b>' +
        '（官方措辞：<code>]</code> 必须是和弦部分的最后一个字符，而和弦部分永远排在终止式和乐句标记前面）；' +
        '② <b><code>}</code> 永远是整条标签的最后一个字符</b>。' +
        '官方文档给过一个"理论上最长的标签"：<code>bvii.V[#viio65(+b84)/#vi</code>——' +
        '局部调、持续音、罗马数字、和弦形态、数字低音、变化音、相对根全用上了。' +
        '它是纯示例，但正好让你看见一条标签能装多少东西。</p>' +
        '<p><b>为什么调信息只写一次？</b>因为标签定义的是<b>半开区间</b>：一个调或一个和声从标签所在的时间点开始生效，' +
        '一直持续到下一个标签为止。所以全局调只写在第一个标签上，局部调只在<b>变化</b>的那一条上写，' +
        '其余标签全部继承。<b>这也是读别人标注文件时不能只看一行的原因</b>——' +
        '单独摘出一行 <code>V7</code>，你并不知道它在哪个调里。</p>' },

      { type: 'terms', title: '十个字段：它们对应官方正则的命名组', items: [
        ['globalkey 全局调', 'global key', '写作小写音名，如 <code>Ab</code>、<code>c#</code>。形如 Ab.vi.i 的开头',
          '<b>为什么成立：</b>标签是半开区间的分段——官方技术规格写明，只要第一个和弦标签带了调信息，' +
          '这个调就对它后面所有标签生效，直到出现新的调指示。所以全局调<b>整份谱只写一次</b>，写在第一个标签上。' +
          '音名的<b>大小写就是调式</b>：大写 <code>Ab</code> 是降 A <b>大调</b>，小写 <code>c#</code> 是升 c <b>小调</b>。' +
          '写法上音名后面必须跟一个点：<code>f#.i</code> 是升 f 小调的第一个主和弦，<code>Ab.I</code> 是降 A 大调的主和弦。<br>' +
          '<b>容易混淆：</b>① 以为每条标签都要写全局调——不用，只在第一条；' +
          '② 把调号当调性：调号只告诉你"某个大调或它的关系小调"，究竟是哪一个必须看音乐本身' +
          '（C 大调和 a 小调共用一个空调号）。<br>' +
          '<b>例子：</b><code>Ab.vi.i</code>——降 A 大调的作品，开头却是在关系小调 f 小调上的引子；' +
          '这是官方发布说明里举的原例。'],

        ['localkey 局部调', 'local key', '罗马数字表示，如 <code>V</code>、<code>vi</code>。表示此处已转入该调',
          '<b>为什么成立：</b>局部调一律<b>相对全局调</b>计算，写法是"音级 + 点 + 相对新调的和弦"，' +
          '如 <code>V.I</code>（此处转入属调）。官方特意提醒：正因为它是相对的，' +
          '大调作品里 <code>III</code>／<code>VI</code>／<code>VII</code> 离主音是<b>大</b>三度、大六度、大七度，' +
          '小调作品里则是小三度、小六度、小七度——同一个 <code>III</code> 在不同作品里指的调并不一样。<br>' +
          '<b>它不是每条都要写：</b>官方措辞是"它为所附的和弦、以及其后<b>直到下一次调指示为止</b>的所有符号设定语境"。<br>' +
          '<b>最容易错的两点：</b>① <b>点（<code>.</code>）和斜杠（<code>/</code>）算的不是同一个调</b>——' +
          '点相对全局调，斜杠相对局部调。官方专门发过警告：局部调是 <code>V</code> 时写 <code>V7/IV</code>，' +
          '指的是"局部调的 IV 级"（也就是全局主调）的属七，跟 <code>I.V7</code> 完全不是一回事；' +
          '② 一见离调就改局部调。官方的取舍是：短暂的 tonicization 用斜杠连写成一组' +
          '（<code>iio65/vi V7/vi i/vi</code>），只有音乐<b>真的留在新调</b>里才改局部调；' +
          '改的时机官方给了一条两头夹的规则——最晚不能晚于"某个和弦只能用新调解释"的那一刻，最早则要与前后文保持一致。<br>' +
          '<b>例子：</b>官方教程用四种等价写法示范同一个进行（从全程 <code>viio/V V</code>，到中途换成 <code>V.V7 I</code>），' +
          '和弦没变，变的是你声称"从哪一拍起算换了调"。'],

        ['pedal 持续音', 'pedal point', '写作 <code>V[</code>，与 <code>]</code> 配对结束',
          '<b>为什么成立：</b>它标的是一个低音持续音（organ point）的起止。<code>[</code> 前面写低音所在的音级，' +
          '紧跟着写持续音上的第一个和弦，所以最常见的形状是 <code>I[I</code>、<code>V[V</code>、<code>i[V7/iv</code>。<br>' +
          '<b>判据（两条必须同时满足，否则不许用）：</b>① 同一个低音上至少有三个<b>不同的</b>和声事件；' +
          '② 其中至少有一个和声<b>不包含</b>这个低音。据此，<code>I I(64) I</code> 这类 5/3–6/4–5/3 的邻音运动' +
          '<b>不算</b>持续音——只有两个不同事件，而且低音始终是和弦音，官方明确说这是"隐含的"邻音进行。<br>' +
          '<b>容易错：</b>① 只写 <code>V[</code> 不写 <code>]</code>——方括号不能单独成条，必须和弦标签绑定；' +
          '② 把 <code>]</code> 写在终止式或乐句标记后面——正确形状是 <code>V[V7]|PAC}</code>；' +
          '③ 持续音中途改了局部调——官方要求用前一条标签结束旧的持续音，再另开一条新的。<br>' +
          '<b>例子：</b>官方给的形状 <code>I[I IV64 V7 I]</code>，以及持续音上和声怎么写的取舍规则：' +
          '低音是和弦音时写与低音对应的转位，否则写上方和声的转位——但更重要的规则压过它：' +
          '持续音上方的和声进行自身要说得通。'],

        ['numeral 罗马数字', 'numeral', '大调大三、小调小三：<code>I ii iii IV V vi vii</code>；可加 <code>b/#</code> 前缀',
          '<b>为什么成立：</b>大小写不是排版习惯，是<b>三音</b>的信息——大写 = 大三度，小写 = 小三度；' +
          '五音与七音再由 <code>o</code>／<code>+</code>／<code>%</code>／<code>M</code> 补足。' +
          '官方把这条说得很死：一个孤零零的罗马数字只代表该音级上的<b>原位大三或小三和弦</b>，' +
          '<b>其他所有和弦</b>都必须再加符号。加 <code>b</code>／<code>#</code> 改的是"音级相对当前调的音阶"，' +
          '例如 C 大调里 <code>bVI</code> 是降 A 大三和弦、<code>#iv</code> 是升 f 小三和弦。<br>' +
          '<b>最容易错的一条（官方专门列为 Common mistake）：</b>变音号看的是"这个音级在<b>当前调</b>里有没有被改过"，' +
          '<b>不是</b>"这个音在键盘上有没有黑键"。所以 <code>viio/V</code> 不能写成 <code>#viio/V</code>——' +
          '升 f 是 <b>V 那个调（G 大调）里的自然第 7 级</b>，它之所以看起来像"升过"，只是因为你在拿 C 大调比。' +
          '官方给的四种正确写法是 <code>I.viio/V</code>、<code>i.viio/V</code>、<code>I.#viio/v</code>、<code>i.#viio/v</code>。' +
          '注意后两个：斜杠后面写小写 <code>v</code>（小调）时才需要 <code>#</code>。<br>' +
          '<b>还有两件容易忽略的事：</b>① 和弦类型永远写在转位<b>前面</b>（<code>III+6</code>，不是 <code>III6+</code>）；' +
          '② 罗马数字这个位置除了 I–VII，还能填 <code>Ger</code>／<code>It</code>／<code>Fr</code>（增六和弦简写）' +
          '和 <code>@none</code>（确实推断不出和声时用）。'],

        ['form 和弦形态', 'chord form', '<code>%</code> 半减七　<code>o</code> 减　<code>+</code> 增　<code>M</code> 大七　<code>+M</code> 增七大七',
          '<b>为什么成立：</b>这几个符号管的是两件不同的事——<code>o</code> 和 <code>+</code> 描述<b>三和弦本身</b>' +
          '（五音被减或被增），<code>%</code>、<code>M</code> 描述<b>七音</b>，<code>+M</code> 则是两件事叠起来。' +
          '所以 <code>+7</code> <b>不是</b>"增七度"：官方规定 <code>+7</code> 表示增三和弦 + <b>小</b>七度' +
          '（官方表里叫 augmented minor seventh，例 <code>V+7</code>）；要写增三和弦 + 大七度必须用 <code>+M7</code>' +
          '（例 <code>I+M7</code>）——这个组合是 v2.2.0 专门补上的。<br>' +
          '<b>容易混淆：</b>① 把 <code>o</code> 直接当成"减七和弦"。<code>viio</code> 是减三和弦，' +
          '而 <code>o7</code>／<code>o65</code>… 里的那个七音按标准约定是<b>减</b>七度，所以 <code>viio7</code> = 全减七；' +
          '要让七音是小七度（半减七）必须换成 <code>%</code>（<code>vii%7</code>）。' +
          '官方举的例子：在 D 大调里 <code>viio7</code>（C#-E-G-Bb）与 <code>vii%7</code>（C#-E-G-B）' +
          '差的正是那个七音，而 Bb 分明不是 D 大调的自然音——写作 <code>viio7</code> 而不是 <code>vii%7</code> 就已经把它说清楚了。' +
          '② 把 <code>M</code> 当成"大调"。<code>M</code> 只说明七音是大七度，放在小写罗马数字上就是<b>小三大七</b>和弦' +
          '（<code>iiiM7</code>，官方叫 minor major seventh）。<br>' +
          '<b>例子：</b><code>vii%7</code> 与 <code>viio7</code> 只差一个符号，音响差半个音；' +
          '<code>I+M7</code> 与 <code>I+7</code> 同理。标错一个字母，语料统计出来的和弦类型就是错的。'],

        ['figbass 数字低音', 'figured bass', '<code>7 / 65 / 43 / 42 / 2 / 64 / 6</code>',
          '<b>为什么恰好是这七个值？</b>数字低音的原则是"从<b>低音</b>往上数出和弦音"（就是 A1 那套）。' +
          '三和弦原位是 5/3，既然人人都知道，就<b>不必写</b>——所以取值里<b>没有 5 和 3</b>，原位的写法就是什么都不写。' +
          '剩下的：三和弦两种转位 <code>6</code>（三音在低音：根音在六度上）、<code>64</code>（五音在低音：三音在六度、根音在四度）；' +
          '七和弦四种位置全都要写：<code>7</code>（根音在低音）、<code>65</code>（三音在低音：根音 6 度、七音 5 度）、' +
          '<code>43</code>（五音在低音：七音 4 度、根音 3 度）、<code>2</code>／<code>42</code>（七音在低音：三音 4 度、根音 2 度）。<br>' +
          '<b>官方的记忆钩子：阿拉伯数字永远降序排列。</b>所以是 <code>65</code> 不是 <code>56</code>、' +
          '<code>64</code> 不是 <code>46</code>、<code>43</code> 不是 <code>34</code>。' +
          '官方还专门注明：<code>2</code> 才是首选写法，<code>42</code> 只是被接受。<br>' +
          '<b>容易错：</b>① 给原位三和弦补一个 <code>53</code>（<code>V53</code>）——取值表里没有它，正则会拒；' +
          '② 写 <code>46</code>；③ 写 <code>V9</code>——从 v2.0.0 起 <code>9</code> 不再是合法的 <code>figbass</code>，' +
          '九音必须写成变化音：<code>V7(9)</code> 或 <code>V7(+9)</code>（这一点在 E3 练习 3 还会遇到）。<br>' +
          '<b>例子：</b>同一个 V7，低音依次是 G／B／D／F，就依次写作 <code>V7</code>／<code>V65</code>／<code>V43</code>／<code>V2</code>。' +
          '<b>只有低音能决定 figbass，上方怎么排列不影响它。</b>'],

        ['changes 变化音', 'chord tone changes', '括号内，如 <code>I(^2)</code>、<code>V(+4)</code>。<code>^</code> 表示替换上方音，<code>v</code> 表示替换下方音',
          '<b>先记住一件最关键的事：括号里的数字和 <code>figbass</code> 数的不是同一个东西。</b>' +
          '<code>figbass</code> 从<b>低音</b>往上数且与转位绑定；括号里的数字从<b>根音</b>往上数，' +
          '<b>与转位无关</b>——官方原话是 <code>(4)</code> 在 <code>V(4)</code>、<code>V6(4)</code>、<code>V65(4)</code>、' +
          '<code>V43(4)</code>、<code>V2(4)</code> 里意思完全一样（其中两个的挂留音还落在低音）。' +
          '这是从数字低音思维转过来最容易翻车的地方。<br>' +
          '<b>默认含义：</b>光写数字表示"这个音替换掉某个和弦音"。<code>(2)</code> 替换根音、<code>(4)</code> 替换三音、' +
          '<code>(6)</code> 替换五音，被替换的那个音<b>不在</b>；换成 <code>(9)</code>／<code>(11)</code>／<code>(13)</code>，' +
          '意思就变成"那个和弦音仍在，只是高八度那份被替换掉了"（"Chopin 和弦"写成 <code>V7(13)</code> 就是这个道理）。<br>' +
          '<b><code>^</code> 和 <code>v</code> 说的是"这个数字去替换谁"：</b><code>^</code> = 替换它<b>上方</b>那个和弦音，' +
          '所以 <code>^2</code> 是用二度<b>从下方</b>替换三音（官方英文叫 replacement from below），' +
          '这是<b>上行解决</b>的延留音 retardation；<code>v</code> = 替换它<b>下方</b>那个和弦音，' +
          '例 <code>v#2</code> 用升高的上方邻音替换根音。' +
          '<b>英文的 from above／from below 说的是"替换音从哪边来"，正好和"替换掉上面还是下面那个音"相反</b>，' +
          '读官方文档时几乎人人都会搞反一次——记住 <code>^2</code> 这个例子就够了。<br>' +
          '<b>还有一个默认规则：</b>数字带变音号时方向默认已定——<code>b</code> 默认"从上方替换"（挂留音），' +
          '<code>#</code> 默认"从下方替换"（延留音）。所以要写"升高的挂留音"就必须显式加 <code>v</code>（<code>v#2</code>）。<br>' +
          '<b>另外三个符号：</b><code>+</code> 是<b>添加</b>而非替换（<code>V6(+6)</code>）；<code>-</code> 是"这个和弦音<b>缺失</b>且不被隐含"' +
          '（<code>I(-3)</code> 表示明显的空五度）；多个变化音必须写在同一个括号里并<b>降序</b>排列，' +
          '如 <code>i(9#74)</code>，不能写 <code>i(#742)</code>。<br>' +
          '<b>例子：</b>终止四六写作 <code>V(64)</code>，经过的 I64 写作 <code>I64</code>——这对写法就是靠 <code>changes</code> 与 ' +
          '<code>figbass</code> 的分工区分开的。'],

        ['relativeroot 相对根', 'relative root', '斜杠后写，如 <code>V7/V</code>、<code>viio7/bIII</code>',
          '<b>怎么读：斜杠后面是"参照系"，斜杠前面的和弦按那个调来读。</b>' +
          '<code>V7/V</code> 读作"在 V（属调）里的 V 级七和弦"，也就是重属七和弦。' +
          '斜杠后面同样用大小写区分参照调的大小调：<code>V7/V</code> 的 V 是大调，<code>#viio/v</code> 的 v 是小调。<br>' +
          '<b>为什么容易错：</b>① 以为斜杠改了调——它<b>不改</b>，只表示"向某个调借"，局部调要改必须用点；' +
          '② 忘了斜杠算的是<b>局部调</b>：局部调是 <code>V</code> 时，<code>V7/IV</code> 指的是"局部调的 IV 级"（也就是全局主调）的属七；' +
          '③ 变音号写错（见 <code>numeral</code> 那条的 <code>#viio/V</code>）；' +
          '④ 斜杠后面加点——旧版允许 <code>V/.bIII</code>，现在被禁止，正确写法是 <code>V/bIII</code>。<br>' +
          '<b>可以套多层，也可以连写：</b>官方允许不定层数（<code>viio/V/v</code>），也允许把一小段小调性化连写成一组' +
          '（<code>iio65/vi V7/vi i/vi</code>），让"这是短暂借用"在视觉上就是一组。<br>' +
          '<b>容易混淆的取舍：</b>同一个音响（C 大调里的 D-F#-A-C）既可以写成 <code>II7</code>，也可以写成 <code>V7/V</code>。' +
          '官方的规则是：如果它后面<b>跟着它自己的主和弦</b>，就应写成次级属和弦 <code>V7/V</code>。<br>' +
          '<span class="sub">一处笔误提醒：上面判据列的 <code>vii°7/bIII</code> 是为了可读性用了乐理记号 <code>°</code>，' +
          '但官方正则只认字母 <code>o</code>，真写标签时必须写成 <code>viio7/bIII</code>' +
          '（半减七则是 <code>vii%7/bIII</code>）——否则会被判为语法错误。</span>'],

        ['cadence 终止式', 'cadence', '竖线后写：<code>PAC IAC HC DC EC PC</code>',
          '<b>挂在谁身上：</b>终止式标签挂在<b>终止极音</b>（ultima）上，也就是终止式的最后一个和弦——' +
          '所以 PAC／IAC 挂主和弦、HC 才挂在 V 上（E2 会细讲这个判据）。写法是"和弦 + 竖线 + 标签"，如 <code>V|HC</code>。<br>' +
          '<b>为什么位置能分开：</b>如果 ultima 的解决被挂留音推迟，终止式标签标在它<b>完成</b>的那一刻，' +
          '而结构位置由 <code>}</code> 标出——两者可以不在同一拍上，官方教程特意举过这种例子。' +
          '终止式标签也能不带和弦单独出现（如 <code>|EC</code>），但很罕见。<br>' +
          '<b>最容易错的一条：</b>终止式与乐句结束同时出现时，<code>}</code> <b>放最后</b>（官方例子 <code>i|PAC}</code>）——' +
          '见下面练习 3。<br>' +
          '<b>一处需要你知道的分歧：</b>"官方正则"其实存在两个版本，而 <code>PC</code> 正好卡在这个差别上。' +
          '官方教程与 Reference 的表里都列着 PC（Reference 注明它"被接受但应谨慎使用，' +
          '变格终止对哪些曲目算一个分析范畴仍有争议"），官方 Technical Specifications 页引用的正则里也<b>有</b> PC' +
          '（本站检查器用的就是这一版）；但仓库里发布的 <code>harmony.py</code>（master／docs 分支）' +
          '终止式组只写到 <code>EC</code>，<b>没有 PC</b>——照那份文件编译，<code>V|PC}</code> 会被判为语法错误。<br>' +
          '<span class="sub">附一个核对时发现的、更需要小心的坑：那份发布的 <code>harmony.py</code> 里，' +
          '终止式组前面的竖线还漏了转义（写作 <code>(|…</code>，而不是文档里的 <code>(\\|…</code>）。' +
          '照它原样编译，<b>连 <code>V|IAC}</code> 都匹配不上</b>——竖线被当成了"空分支"的分隔符。' +
          '官方教程与 Reference 的例子、以及 Technical Specifications 页引用的正则，都要求写这个竖线；' +
          '所以写法以文档为准（本站检查器接受的正是文档那一版）；' +
          '但要记住：<b>"以正则为准"必须先说清是哪一版正则</b>。</span>'],

        ['phraseend 乐句标记', 'phrase mark', '<code>{</code> 乐句开始，<code>}</code> 结构结束，<code>}{</code> 交叠',
          '<b>它是什么：</b>一套<b>非层次</b>的分组标记。官方特意声明这里的 phrase <b>不是</b>申克意义上的乐句，' +
          '它大致相当于演奏者换气、排练时对起点用的标点——所以它和 C 模块学的功能段不是一回事，' +
          '官方也把"用在哪一层"留给标注者自己决定并要求前后一致。<br>' +
          '<b>判据：</b><code>{</code> 放在乐句的<b>第一个事件</b>上（和声没变时可以只写一个 <code>{</code> 单独成条）；' +
          '<code>}</code> 放在<b>感知到的结构性结束点</b>上，<b>不一定在最后一个音</b>——' +
          '官方的例子里 <code>}</code> 落在第 1 拍，后面还有一小段余力。<br>' +
          '<b>为什么这样设计：</b><code>}</code> 之后、下一个 <code>{</code> 之前的材料仍然属于<b>同一个乐句</b>' +
          '（小结尾、连接、休止都算），所以算乐句长度要看 <code>{</code> 到 <code>{</code>，不是 <code>{</code> 到 <code>}</code>。<br>' +
          '<b>容易错：</b>① 把 <code>}</code> 放在乐句最后一个音上；② 把 <code>}</code> 插到终止式前面' +
          '（<code>V}|IAC</code>）——顺序永远是"和弦 → <code>]</code> → 终止式 → 乐句标记"；' +
          '③ 只写 <code>}</code> 忘了 <code>{</code>：官方说括号配平是这套语法里<b>最常见的错误来源</b>，' +
          '而 <code>{</code> 少写不会报错，只会让你的乐句长度全错；' +
          '④ 把 <code>}{</code> 当成"结束 + 开始"两条——它只在<b>前一句的结尾正好是后一句的开头</b>（交叠）时才用。<br>' +
          '<b>例子：</b><code>I{</code> … <code>I|PAC}</code>；半终止收尾时是 <code>V|HC}</code>。']
      ]},

      { type: 'prose', title: '标签是叠加的：从一层信息读到四层信息', html:
        '<p>下面五条都是真标签。一条一条读，看它比上一条多装了哪一层。' +
        '<b>读的顺序永远是：先找点（调），再找斜杠（参照调），再看括号（变化音），最后看末位（结构）。</b></p>' +
        '<ul>' +
        '<li><code>Ab.vi.i</code> — <b>三层：全局调 + 局部调 + 和弦。</b>' +
        '降 A 大调的作品，此处局部调为 vi（f 小调），和弦是 i（f 小调主和弦）。' +
        '这是"引子从关系小调开始"的标准写法。<b>容易错：</b>把 <code>vi</code> 当成一个绝对调名——' +
        '它是相对 <code>Ab</code> 算出来的音级，换成别的全局调，同一个 <code>vi</code> 就指向别的调。</li>' +
        '<li><code>V7/V</code> — <b>两层：和弦 + 参照调。</b>斜杠后面是参照系，' +
        '读作"V（G 大调）里的 V7"，也就是 C 大调里的重属七（D-F#-A-C）。' +
        '<b>容易错：</b>写成 <code>#V7/V</code>——升 f 是 V 那个调里的自然音，不加号；' +
        '以及忘了斜杠<b>不改</b>局部调，只表示借。</li>' +
        '<li><code>I(^2)</code> — <b>两层：和弦 + 替换。</b>' +
        '<code>^</code> 说的是"这个二度去替换它<b>上方</b>那个和弦音"，也就是用二度<b>从下方</b>替换三音；' +
        '因为替换音在下方、解决向上，它是<b>延留音的反向形态 retardation</b>，' +
        '而官方引入 <code>^</code> 这个符号，正是为了把 retardation 和普通挂留音区分开。' +
        '<b>容易错：</b>① 把它当成普通挂留音——挂留音是<b>下行</b>解决的，默认写法是 <code>I(2)</code>' +
        '（二度替换根音），<code>I(2)</code> 和 <code>I(^2)</code> 的差别就是"替换根音"还是"替换三音"，' +
        '也就是"下行解决"还是"上行解决"；② 以为括号里的数字和 <code>figbass</code> 一样从低音数——' +
        '<b>不是，括号里从根音数</b>，与转位无关。</li>' +
        '<li><code>V|HC}</code> — <b>三层：和弦 + 终止式 + 结构位置。</b>' +
        'V 级和弦；<code>HC</code> 是半终止，挂在这个终止极音上；<code>}</code> 表示这里是乐句的结构性结束点。' +
        '一条标签同时给出了和声、终止式、结构三层信息。<b>容易错：</b>顺序写成 <code>V}|HC</code>——' +
        '<code>}</code> 必须最后（练习 3 有完整说明）。</li>' +
        '<li><code>Fr43</code> — <b>一个符号代替一串。</b>法国增六和弦的简写，' +
        '等价的完整写法是 <code>V43(b5)/V</code>（官方对照表：<code>Fr</code> 的根写作 <code>V/V</code>，' +
        '标准数字低音是 43）。同族的还有 <code>It6</code> = <code>viio6(b3)/V</code>、' +
        '<code>Ger6</code> = <code>viio65(b3)/V</code>。<b>容易错：</b>以为简写和完整写法是两种和弦——' +
        '它们是同一个音响的两种写法，官方解析器会把简写直接翻译成完整写法。' +
        '另外别把它和普通数字低音混起来：<code>Fr43</code> 里的 43 确实是数字低音，' +
        '但这个和弦本身是"降了五音的属和弦"，光看 43 看不出这件事，必须靠简写或括号。</li>' +
        '</ul>' },

      { type: 'prose', title: '会被正则拒掉的写法，以及它们各自的正确写法', html:
        '<p>下面这些是新手最常写、而官方正则<b>会直接判为语法错误</b>的形态。' +
        '它们都不涉及"分析对不对"，只是语法——好消息是这种错一定能自查出来：</p>' +
        '<ul>' +
        '<li><code>V}|HC</code> → 写 <code>V|HC}</code>。乐句标记永远是最后一个字符。</li>' +
        '<li><code>V53</code>（给原位三和弦补数字）→ 写 <code>V</code>。取值表里没有 <code>5</code> 和 <code>3</code>。</li>' +
        '<li><code>V9</code> → 写 <code>V7(9)</code> 或 <code>V7(+9)</code>。<code>9</code> 不是合法的 <code>figbass</code>（v2.0.0 起）。</li>' +
        '<li><code>IV46</code> → 写 <code>IV64</code>。阿拉伯数字永远降序。</li>' +
        '<li><code>V7/.bIII</code> → 写 <code>V7/bIII</code>。斜杠后面不许再跟点。</li>' +
        '<li><code>vii°7/bIII</code> → 写 <code>viio7/bIII</code>。<code>°</code> 是乐理记号，' +
        '正则只认字母 <code>o</code>（半减七写 <code>%</code>）。</li>' +
        '<li>圆括号里的变化音没闭合：<code>V(4</code>——<code>changes</code> 的圆括号是唯一会让正则直接失败的括号。</li>' +
        '</ul>' +
        '<p><b>还有一类错和上面这些性质不同：正则会放它过去。</b><code>#viio/V</code> 语法上完全合法，' +
        '但分析上是错的（见上面 <code>numeral</code> 那条）。同类的还有 <code>V[V7</code>（持续音少了 <code>]</code>）' +
        '和孤零零一个 <code>}</code>（少了配对的 <code>{</code>）——正则里这两处都是可选的，所以它不报错；' +
        '但官方教程明确要求方括号成对，而少写 <code>{</code> 会让你的乐句长度全部算错。' +
        '<b>正则只管拼写，不管你对不对</b>——语法过了不等于标注对了，两件事都要自己盯。</p>' },

      { type: 'viz-tonnetz',
        title: '标签背后的几何：音网 Tonnetz',
        note: '罗马数字不是随便起的名字——它描述的是和弦在<b>五度与三度关系网</b>里的位置。' +
              '这个网就是音网：横向是纯五度关系，斜向是大三度/小三度关系。' +
              '<b>相邻的三角形共享两个音</b>，所以它们在听觉上"很近"。',
        progression: [
          { label: 'I（C 大三）', root: 0, quality: 'M' },
          { label: 'vi（a 小三）', root: 9, quality: 'm' },
          { label: 'IV（F 大三）', root: 5, quality: 'M' },
          { label: 'V（G 大三）', root: 7, quality: 'M' },
          { label: 'I（C 大三）', root: 0, quality: 'M' }
        ],
        stepMs: 1000,
        caption: '点「走一遍和声进行」，看高亮三角形怎么在网里移动。' +
                 '<b>留意每次移动共享了几个音</b>：I→vi 共享两个音（C、E），只动一个声部，' +
                 '所以听起来很顺；而 I→IV 移动的步子更大。' +
                 '这就是为什么 vi 是 I 最自然的"代替品"，也是欺骗终止 DC（V→vi）为什么能骗到耳朵。' },

      { type: 'note', html:
        '<b>音网能帮你理解标注，但它不是标注工具。</b>' +
        'DCML 标签仍然是线性的罗马数字串——因为音乐是时间艺术，标注要跟着时间走。' +
        '音网的价值在于让你<b>看见关系</b>：为什么 bVI 在关系小调里那么自然、' +
        '为什么那不勒斯六和弦离主调"不远"、为什么半音化声部进行总是走最短路径。' +
        '这些直觉，靠读标签读不出来，靠看图能看出来。' },

      { type: 'drill', drill: 'label', title: 'E1 练习 1：写一个重属和弦',
        notes: '<D4 F#4 A4 C5>:2',
        context: 'C 大调。低音是 D，这是"属和弦的属和弦"（重属七和弦），原位。请写出 DCML 标签。',
        answers: ['V7/V'],
        placeholder: '提示：用斜杠表示相对根',
        why: '根音 D 是 C 大调 V 级（G）的 V 级 → 相对根写作 <code>/V</code>；' +
             'D 在低音且是七和弦原位 → <code>figbass = 7</code>。合起来是 <code>V7/V</code>。' },

      { type: 'drill', drill: 'label', title: 'E1 练习 2：带变化音的和弦',
        notes: '<C4 E4 G4 A4>:2',
        context: 'C 大调。这是一个 I 级和弦，但三音 E 被二度 D 替换……等等，先把实际音写清楚：' +
                 '这里的音是 C、E、G、A。A 是附加的六度音。请思考：DCML 会怎么记录这个"额外的音"？' +
                 '（试试把 A 理解为对某个和弦音的替换。）',
        answers: ['I(^2)'],
        placeholder: '格式：I(变化音)',
        why: '严格的 DCML 里没有"add6"这种写法——标准要求把每个非和弦音解释为对某个和弦音的<b>替换</b>。' +
             '附加六度通常被记录为对五音的替换或对某音的添加，具体取决于上下文与声部走向。' +
             '这正是 DCML 与传统和声标记最大的差异：<b>它逼你解释每一个音的来路</b>，' +
             '而不是含糊地写个"附加音"。建议对照官方 <a href="https://dcmlab.github.io/standards/build/html/reference/reference.html" target="_blank" rel="noopener">Annotation Reference</a> 确认此处的最佳写法。' },

      { type: 'drill', drill: 'label', title: 'E1 练习 3：把终止式与乐句写进标签',
        notes: '<G3 B3 D4>:1 <C4 E4 G4>:3',
        context: 'C 大调，V → I，均为原位，最高声部停在 E（I 的三音）。这是一个乐句的结构性结束点。',
        answers: ['V|IAC}'],
        placeholder: '把和弦、终止式、乐句标记都写进去',
        why: '高音停在 I 的三音 E 上，不是主音 → 不满足 PAC 的第三个条件 → <b>IAC</b>（判据见 A2）。' +
             '终止式用竖线 <code>|</code> 接在和弦后面，写作 <code>V|IAC</code>。' +
             '<b>乐句标记 <code>}</code> 必须放在整条标签的最后</b>，所以完整写法是 <code>V|IAC}</code>。<br>' +
             '<b>为什么 <code>}</code> 必须最后？</b>官方 Reference 与官方教程都写明了同一条规则：' +
             '"终止式标签与乐句结束同时出现时，<code>}</code> 放最后"，官方自己的例子是 <code>i|PAC}</code>。' +
             '原因在正则的结构里：一条标签被拆成"和声部分（调 → 持续音 → 和弦 → 终止式）+ 乐句部分"，' +
             '乐句标记是<b>最后那个命名组</b>。所以 <code>V|IAC}</code> 能解析，' +
             '而 <code>V}|IAC</code> 把 <code>}</code> 塞进了中间——正则接不上后面的 <code>|IAC</code>，直接判为语法错误' +
             '（你可以在本站的标签检查器里输了试试——它用的正是文档那一版官方正则）。<br>' +
             '<span class="sub">再补一个真实的坑：DCML 仓库 README 的 v2.3.0 发布说明里，例子恰好写成了 ' +
             '<code>V}|HC</code>——和官方教程正文、官方 Reference、官方正则三处都矛盾' +
             '（这三处说的都是"<code>}</code> 放最后"）。<b>以正则为准</b>：' +
             '正则才是真正用来解析标签的东西，一句发布说明不是。</span>' },

      { type: 'note', html:
        '<b>兼容性提醒（重要）：</b>DCML 官方教程明确要求 <b>MuseScore 3.6.2</b> 并以 <code>.mscx</code>' +
        '（未压缩）格式保存，其配套工具 <code>ms3</code> 是按 MS3 文件格式写的。' +
        '你装的是 <b>MuseScore 4.4.4</b>。实际影响与对策：' +
        '<ul>' +
        '<li><b>标注本身能做</b>：MuseScore 4 仍有「添加 → 文本 → 罗马数字分析」功能，录入方式一致。</li>' +
        '<li><b>保存格式不同</b>：MS4 保存的 <code>.mscx</code> 结构有变化，早期版本的 ms3 可能读不了。' +
        'ms3 2.x 已支持 MS4，但需要联网安装（当前环境 pip 走代理被拒，暂装不了）。</li>' +
        '<li><b>应急方案</b>：MuseScore 4 可以导出 MusicXML，而 MusicXML 是通用格式，' +
        '用标准库就能解析出你录入的标签——本站的标签检查器走的就是这条路。</li>' +
        '</ul>' },

      { type: 'prose', title: '标注工作流（你现在就能跑通的版本）', html:
        '<p>语法必须落到手上，否则读完就忘。下面是按你现在的软件条件能跑通的流程，' +
        '每一步都写了"为什么是这一步"：</p>' +
        '<ol>' +
        '<li>在 MuseScore 4 里打开一段谱（先用贝多芬 Op. 18 或任何你熟悉的曲子）。' +
        '<b>为什么先挑熟悉的曲子：</b>标注要同时判断调、转位、终止式三件事，' +
        '用陌生的曲子会把"读谱"和"判断"混在一起，出错时分不清是哪一步错了。</li>' +
        '<li>选中音符 → 「添加 → 文本 → 罗马数字分析」，逐拍录入 DCML 标签。' +
        '<b>必须录在「罗马数字分析」这一层</b>，不要用「和声」层、StaffText 或歌词——' +
        '官方明确要求标签录在这一层，录错了工具就提取不出来。</li>' +
        '<li>用 <code>空格</code> 跳到下一个音，<code>Tab</code> 跳到下一小节。' +
        '这两个键决定你标注的速度，先练熟再标长曲子。</li>' +
        '<li>把标签内容整理成一行行文本，贴进本站的标签检查器，或跑命令行校验语法。' +
        '<b>这一步不能省</b>：人眼看不出的顺序错误（比如把 <code>}</code> 写在终止式前面）正则会立刻抓出来。</li>' +
        '<li>把标注结果存成自己的语料库，以后可以回头统计自己的标注习惯。' +
        '这也是 E4 的起点：同一套字段，先用在别人的曲子上，再用在自己的素材上。</li>' +
        '</ol>' +
        '<p><b>动手前先定"和声节奏"。</b>官方把这件事称为标注者之间分歧的<b>最大来源</b>：' +
        '一个小节写一个和弦，还是每拍都写？先为整首曲子定一个粒度，然后<b>始终一致</b>。' +
        '一份粒度不一致的标注，比一份粒度粗糙但一致的标注更没用——因为前者没法统计。' +
        '官方教程让学生对着同一首 Corelli 做粗粒度和细粒度两套标注并比较，' +
        '就是为了让你亲身体会这个决定有多大影响。</p>' },

      { type: 'repertoire', title: '真实文献任务', intro:
        '最好的老师是已经标好的语料。先读别人的，再自己标，最后对照。',
        items: [
          { work: 'Annotated Beethoven Corpus（ABC）', bars: '贝多芬全部弦乐四重奏',
            task: '这是 DCML 官方标注的真实语料。下载后挑一个乐章，只看标签不看谱，' +
                  '试着从标签反推出音乐的走向——这是训练"标签阅读能力"的捷径。',
            link: 'https://github.com/DCMLab/ABC', linkText: 'GitHub' },
          { work: 'DCML 官方标注教程', bars: '全文',
            task: '逐节做完教程里的练习（蓝色的可展开答案框）。这是投入产出比最高的一件事——' +
                  '它把两年多的专家讨论压缩成了几页。',
            link: 'https://dcmlab.github.io/standards/build/html/tutorial/index.html', linkText: '官方教程' },
          { work: 'DCML 标注规范参考', bars: '全文',
            task: '遇到拿不准的写法时查这里。特别是「Feature Processing」一节，' +
                  '解释了三全音和弦、增六和弦等特殊写法的处理规则。',
            link: 'https://dcmlab.github.io/standards/build/html/reference/specs.html', linkText: 'Technical Specifications' }
        ] },

      { type: 'checklist', items: [
        '能默写 DCML 标签的完整结构骨架（含 ] 与 } 的位置）',
        '能读懂 V7/V、I(^2)、V|HC}、Ab.vi.i、Fr43 各是什么意思、各装了几层信息',
        '能区分 figbass（从低音数、与转位绑定）和 changes（从根音数、与转位无关）',
        '能说清 } 为什么必须写在终止式之后（V|IAC} 而不是 V}|IAC）',
        '知道 DCML 与"附加音"这类传统写法的根本差异在哪',
        '在 MuseScore 4 里成功录入过至少 8 小节的罗马数字标签',
        '读完 DCML 官方教程的 Getting started 与 The first label 两节'
      ] }
    ]
  });

})();
