# 深链接回退规则（SPA fallback）
#
# 为什么需要：本站是 hash 路由的单页应用，单元地址形如 /#/u/B1。
# 真实的 /u/B1 在服务器上并不存在，所以"刷新页面"和"把单元地址发给别人"
# 都会 404。这条规则让所有找不到的路径都回 index.html，由前端自己解析。
#
# 哪些平台读这个文件：
#   · Cloudflare Pages  —— 读，放仓库根即可
#   · Netlify           —— 读，放仓库根即可
#   · Vercel            —— **不读**，要用 vercel.json 的 rewrites（见下）
#   · GitHub Pages      —— **不读**，用根目录的 404.html 兜底（已备好）
#
# Vercel 用户请改用 vercel.json：
#   { "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }] }
#
# 注意：这条规则会让"真正不存在的文件"也返回 200 + index.html。
# 比如 /assets/foo.png 缺失时，浏览器会拿到一份 HTML —— 图片加载失败是必然的，
# 但页面不会崩（渲染器的兜底逻辑会把该区块显示成说明卡片）。
# 如果你更希望缺失资源老实返回 404，把下面的 /* 换成 /u/* 与 /t/* 两条。

/*    /index.html   200
