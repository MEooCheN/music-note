/* ==========================================================================
 * site.config.js — 站点身份与上线开关（**改这里就够了**）
 * --------------------------------------------------------------------------
 * 这是全站唯一一份"站点身份"配置。上线时要改的东西几乎都集中在这个文件里，
 * 不需要去 index.html / site.js / 404.html 里到处找。
 *
 * 加载顺序：必须在 theory/core.js 之前引入（见 index.html 底部）。
 * 它不依赖任何东西，只往 window.SITE_CONFIG 上挂一个对象。
 *
 * 改完之后跑一次自检，它会告诉你还有什么没配好：
 *     node tools/check-site-config.js
 * ========================================================================== */
(function (global) {
  'use strict';

  global.SITE_CONFIG = {

    /* ------------------------------------------------------------------
     * 1. 上线地址 —— 部署前必须改这一项
     * ------------------------------------------------------------------
     * 留空时会自动退回"当前页面的地址"，所以本地预览、或还没定域名时
     * 都不会出错；但**分享卡片（og:image）和 canonical 需要绝对地址**，
     * 留空时它们不会生成。
     *
     * 例：'https://mynotes.example.com'        （自定义域名）
     *     'https://你的用户名.github.io'        （GitHub Pages 用户站）
     *     'https://你的用户名.github.io/仓库名'  （GitHub Pages 项目站）
     *
     * 注意：末尾不要加斜杠。子目录部署（项目站）把路径直接写上即可，
     * 下面 site.basePath 会自动从它推导出来。 */
    siteUrl: 'https://meoochen.github.io/music-note',

    /* 部署在域名的子目录下时才需要手填；留空则从 siteUrl 的路径部分自动推导。
     * 例：'/my-notes'  （不要末尾斜杠） */
    basePath: '',

    /* ------------------------------------------------------------------
     * 2. 站点身份 —— 影响标题、分享卡片、搜索结果摘要
     * ------------------------------------------------------------------ */
    name: '复调 · 曲式 · 标注',
    tagline: '个人音乐理论学习笔记',
    /* 用一行话讲清"这是什么、可信到什么程度"。会被写进
       description / og:description / twitter:description。 */
    description: '个人音乐理论学习笔记：严格对位（文艺复兴五种对位）、曲式与功能分析（Caplin）、音乐信息标注（DCML）。含 Fux《Gradus ad Parnassum》公有领域谱例与一个可运行的对位规则检查器。内容为个人草稿，不保证无错。',
    lang: 'zh-CN',
    author: '个人学习笔记',

    /* ------------------------------------------------------------------
     * 3. 分享卡片图 —— 现在是空的，所以分享出去只有纯文字卡片
     * ------------------------------------------------------------------
     * 已经做好一张：assets/share.png（1200×630，正好是分享卡片的推荐尺寸）。
     * 它由 tools/make-share-image.py 生成 —— 一个几何化的五线谱系统，
     * 谱号用的是 MuseScore 导出 SVG 里的【真实】轮廓，配色取站点自己的调色板。
     *
     * 想换图：做一张 1200×630 的 PNG/JPEG 放进 assets/，改下面的路径即可。
     * 想重新生成：python tools/make-share-image.py（只要 Python 3，无第三方包）
     *
     * 注意：**不要用 SVG**。多数社交平台只接 PNG/JPEG，SVG 会被忽略。 */
    shareImage: 'assets/share.png',
    /* 图片的替代文字（无障碍与部分平台的卡片说明） */
    shareImageAlt: '复调 · 曲式 · 标注 — 个人音乐理论学习笔记',

    /* ------------------------------------------------------------------
     * 4. 收录与统计 —— 可选
     * ------------------------------------------------------------------ */
    /* 是否允许搜索引擎收录整站。改成 false 会生成"禁止收录"的 robots 策略。
     * 想"先上线但不被搜到"就设 false。 */
    allowIndexing: true,

    /* 访问统计（可选）。留空则完全不加载任何第三方脚本。
     * 目前只支持 Cloudflare Web Analytics 与 Umami 这类"贴一个脚本地址"的服务：
     * 填了之后 site.js 会把这个 URL 作为 defer 脚本插进页面。
     * 例：'https://static.cloudflareinsights.com/beacon.min.js'
     * 隐私提醒：一旦启用，访客的访问就会发给第三方。 */
    analyticsScript: '',
    analyticsAttrs: {},

    /* ------------------------------------------------------------------
     * 5. 内容相关的可调项
     * ------------------------------------------------------------------ */
    /* 页脚显示的文字。footerLinks 可自由增删。 */
    footerNote: '谱面与声音来自 MuseScore；练习批改由规则检查器在你的浏览器里完成。',
    footerLinks: [
      { text: '关于本站 · 免责声明', href: 'docs/关于本站.html' },
      { text: '全部单元', href: '#/t/all' }
    ]
  };

})(typeof window !== 'undefined' ? window : globalThis);
